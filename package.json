{
  "name": "mplus-eu-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "M+ EU cutoff tracker — static site plus the Cutoff Forecast snapshot service",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\""
  },
  "dependencies": {
    "@netlify/blobs": "^8.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
